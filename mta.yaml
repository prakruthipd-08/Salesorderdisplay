_schema-version: "3.2"
ID: appsalesorderdisplaybatch12
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: appsalesorderdisplaybatch12-destination-content
  type: com.sap.application.content
  requires:
  - name: appsalesorderdisplaybatch12-destination-service
    parameters:
      content-target: true
  - name: appsalesorderdisplaybatch12-repo-host
    parameters:
      service-key:
        name: appsalesorderdisplaybatch12-repo-host-key
  - name: appsalesorderdisplaybatch12-uaa
    parameters:
      service-key:
        name: appsalesorderdisplaybatch12-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: appsalesorderdisplaybatch12_html_repo_host
          ServiceInstanceName: appsalesorderdisplaybatch12-html5-service
          ServiceKeyName: appsalesorderdisplaybatch12-repo-host-key
          sap.cloud.service: appsalesorderdisplaybatch12
        - Authentication: OAuth2UserTokenExchange
          Name: appsalesorderdisplaybatch12_uaa
          ServiceInstanceName: appsalesorderdisplaybatch12-xsuaa-service
          ServiceKeyName: appsalesorderdisplaybatch12-uaa-key
          sap.cloud.service: appsalesorderdisplaybatch12
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: appsalesorderdisplaybatch12-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: appsalesorderdisplaybatch12-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - appsalesorderdisplaybatch12.zip
      name: appsalesorderdisplaybatch12
      target-path: resources/
- name: appsalesorderdisplaybatch12
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: appsalesorderdisplaybatch12-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: appsalesorderdisplaybatch12-destination-service
    service-plan: lite
- name: appsalesorderdisplaybatch12-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: appsalesorderdisplaybatch12-xsuaa-service
    service-plan: application
- name: appsalesorderdisplaybatch12-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: appsalesorderdisplaybatch12-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
